# The missing `retry` command

```txt
             
NAME:
   retry - The missing retry command

USAGE:
   retry [options] -- command

OPTIONS:
   --count value, -c value     Number of retries (default: 1)
   --interval value, -i value  Interval between retries (default: 1s)
   --timeout value, -t value   Timeout for each command run (default: 1s)
   --verbose, -v               Verbose output (default: false)
   --help, -h                  show help
```